# Local dashboard

Run `npm run demo` from the project root and open `http://localhost:4173`. The dashboard is a static portfolio UI backed by fictional marketplace data. Its buttons are intentionally demo-only until a wallet connection is added.
